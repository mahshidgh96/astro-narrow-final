---
import { Icon } from 'astro-icon/components';

interface Props {
  title: string;
  description?: string;
  cover?: string;
  count: number;
  href: string;
  countLabel: string;
}

const { title, description, cover, count, href, countLabel } = Astro.props;
---

<article class="group h-full">
  <a href={href} class="block h-full">
    <div class="relative flex h-full min-h-64 flex-col overflow-hidden rounded-[var(--radius-card)] border border-border bg-card p-6 shadow-[var(--surface-shadow)] transition-all duration-300 ease-out hover:-translate-y-1 hover:border-primary/25 hover:shadow-[var(--surface-shadow-strong)] focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary/25">
      {cover ? (
        <>
          <img src={cover} alt="" class="absolute inset-0 h-full w-full object-cover transition-transform duration-500 group-hover:scale-105" loading="lazy" />
          <div class="absolute inset-0 bg-gradient-to-t from-black/90 via-black/60 to-black/25"></div>
        </>
      ) : (
        <div class="absolute inset-0 bg-primary/5 opacity-0 transition-opacity duration-300 group-hover:opacity-100"></div>
      )}

      <div class="relative z-10 flex min-h-52 flex-1 flex-col">
        <span class:list={[
          'grid h-8 w-8 place-items-center rounded-full border text-sm',
          cover ? 'border-white/15 bg-white/15 text-white backdrop-blur-sm' : 'border-border bg-muted text-primary'
        ]}>
          <Icon name="lucide:bookmark" class="h-4 w-4" />
        </span>

        <div class="mt-auto">
          <h2 class:list={[
            'truncate text-xl font-semibold leading-tight transition-colors',
            cover ? 'text-white' : 'text-foreground group-hover:text-primary'
          ]}>
            {title}
          </h2>
          {description && (
            <p class:list={[
              'mt-2 line-clamp-2 text-sm leading-6',
              cover ? 'text-white/80' : 'text-muted-foreground'
            ]}>
              {description}
            </p>
          )}

          <div class="mt-4 flex items-center gap-1.5 text-xs font-medium">
            <span class:list={[
              'inline-flex items-center gap-1.5',
              cover ? 'text-white/80' : 'text-muted-foreground'
            ]}>
              <Icon name="lucide:file-text" class="h-3.5 w-3.5" />
              {count} {countLabel}
            </span>
          </div>
        </div>
      </div>
    </div>
  </a>
</article>
